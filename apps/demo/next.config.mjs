/** @type {import('next').NextConfig} */
const config = {
  transpilePackages: [
    "@aetherstack/ui",
    "@aetherstack/patterns",
    "@aetherstack/blocks",
    "@aetherstack/tokens",
    "@aetherstack/themes",
    "@aetherstack/utils",
    "@aetherstack/icons",
    "@aetherstack/tailwind-config",
  ],
}

export default config
