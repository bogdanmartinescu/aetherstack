/** @type {import('next').NextConfig} */
const config = {
  transpilePackages: [
    "@aetherstack/ui",
    "@aetherstack/patterns",
    "@aetherstack/blocks",
    "@aetherstack/tokens",
    "@aetherstack/utils",
    "@aetherstack/icons",
  ],
}

export default config
