{
  "name": "@aetherstack/cli",
  "version": "0.1.0",
  "description": "Aether UI CLI — add components from the Aether UI registry into your project",
  "license": "MIT",
  "bin": {
    "aether-ui": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aetherstack/registry-schema": "workspace:*",
    "commander": "^12.0.0",
    "ora": "^8.0.1",
    "picocolors": "^1.0.1",
    "zod": "^3.23.4"
  },
  "devDependencies": {
    "@aetherstack/typescript-config": "workspace:*",
    "@types/node": "^20.12.7",
    "tsup": "^8.0.2",
    "tsx": "^4.9.3",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
