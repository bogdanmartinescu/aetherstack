/** Default public registry URL. Override via aether.json or --registry flag. */
export const DEFAULT_REGISTRY_URL = "https://registry.aetherui.dev"

/** Registry manifest path, relative to the registry base URL. */
export const REGISTRY_MANIFEST_PATH = "/r/registry.json"

/** Config file name the CLI looks for in the project root. */
export const CONFIG_FILE_NAME = "aether.json"

/** Default component output directory (relative to project root). */
export const DEFAULT_COMPONENTS_DIR = "src/components/ui"

/** Default utils output directory. */
export const DEFAULT_UTILS_DIR = "src/lib"
